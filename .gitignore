node_modules/
dist/
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Local report artifacts (not for version control)
*-report.txt

# Blog working drafts live on Desktop, not the repo
tmp/

# Claude Code personal settings — project-specific (never committed)
.claude/settings.local.json
.claude/.auto-sync.*

# Blog-buster audit artifacts — ephemeral per audit, lineage lives in Supabase
.audit-cache/
audit-reports/
